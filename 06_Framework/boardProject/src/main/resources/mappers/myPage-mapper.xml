<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "mybatis-3-mapper.dtd" >
<mapper namespace="myPageMapper">


	<!-- 회원 정보 수정 -->			<!-- Member : 클래스 이름이 아니라 별칭 -->
	<update id="updateInfo" parameterType="Member">
	UPDATE "MEMBER" SET
	MEMBER_NICKNAME = #{memberNickname},
	MEMBER_TEL = #{memberTel},
	MEMBER_ADDR = #{memberAddress}
	WHERE MEMBER_NO = #{memberNo}	
	</update>
	
	<!-- 
	
	[마이바티스 기본 별칭]
	java		mybatis
	int			_int
	Integer		int
	String		string	
	
	 -->
	<!-- 비밀번호 조회 -->							<!-- 조회 결과가 1개인 경우 -->
												<!-- 조회 결과를 모두 Map에 담는 경우 -->
	<select id="selectEncPw" parameterType="_int" resultType="String">
	SELECT MEMBER_PW FROM "MEMBER" WHERE MEMBER_NO = #{memberNo}
	</select>
	
	<!-- 비밀번호 변경 -->
	<!-- Member : 클래스명이 아니라 mybatis-config.xml 파일에 지정한 사용자 별칭 -->
	<update id="changePw" parameterType="Member">
	UPDATE "MEMBER"
	SET MEMBER_PW = #{memberPw}
	WHERE MEMBER_NO = #{memberNo}
	</update>
	
	<!-- 회원 탈퇴 -->
	<update id="secession" parameterType="_int">
	UPDATE "MEMBER"
	SET MEMBER_DEL_FL = 'Y'
	WHERE MEMBER_NO = #{memberNo}
	</update>
	
</mapper>
